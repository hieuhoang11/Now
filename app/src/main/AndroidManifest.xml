<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hieuhoang.now">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/map_api_key" />

        <activity android:name=".View.Main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.HotProduct.HotProductsActivity" />
        <activity android:name=".View.LoginRegister.LoginRegisterActivity" />
        <activity android:name=".View.Store.StoreActivity" />
        <activity android:name=".View.Test.TestActivity" />
        <activity android:name=".View.Test.Test1Activity" />
        <activity android:name=".View.Test.test3Activity" />
        <activity android:name=".View.SubmitOrder.SubmitOrderActivity" />
        <activity android:name=".View.Test.map.MapActivity" />
        <activity android:name=".View.Brand.BrandActivity" />
        <activity android:name=".View.SubmitOrder.EditInfo.EditInfoActivity" />
        <activity android:name=".View.Location.LocationActivity"></activity>
    </application>

</manifest>